{
    "name": "basepath-example",
    "workspaces": [
        "apps/*"
    ],
    "scripts": {
        "build": "turbo run build",
        "dev": "dotenv -- turbo run dev",
        "start": "turbo run start"
    },
    "devDependencies": {
        "turbo": "^1.2.9"
    },
    "license": "MIT",
    "dependencies": {
        "dotenv-cli": "^7.2.1"
    }
}
